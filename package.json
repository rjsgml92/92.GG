{
  "name": "gg92-backend",
  "version": "1.0.0",
  "description": "GG.92 Backend Server",
  "main": "server.js",
  "scripts": {
    "start": "node scripts/sync-public.js && node server.js",
    "check": "node --check server.js && node --check evaluation.js && node --check scripts/sync-public.js",
    "vercel-build": "node scripts/sync-public.js"
  },
  "dependencies": {
    "@google/genai": "^2.16.0",
    "axios": "^1.6.0",
    "cors": "^2.8.5",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "vercel": "^58.9.0"
  }
}
